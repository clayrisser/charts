apiVersion: v1
kind: Secret
metadata:
  name: {{ template "postgres.fullname" . }}-postgres-secret
  labels:
    app: {{ template "postgres.name" . }}-postgres-secret
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
stringData:
  username: postgres
  password: {{ randAlphaNum 24 | quote }}
