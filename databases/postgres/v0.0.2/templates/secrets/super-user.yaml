apiVersion: v1
kind: Secret
metadata:
  name: {{ template "postgres.fullname" . }}-super-user
  labels:
    app: {{ template "postgres.name" . }}-super-user
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
stringData:
  username: postgres
  password: {{ randBytes 24 | quote }}
