apiVersion: v1
kind: Secret
metadata:
  name: {{ template "postgres.fullname" . }}-standard-user
  labels:
    app: {{ template "postgres.name" . }}-standard-user
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
stringData:
  username: ''
  password: ''
