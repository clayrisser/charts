config:
  imagePullPolicy: IfNotPresent
  enableAntiaffinity: false
  postgresOperator:
    resources:
      enabled: defaults
      requests:
        cpu: 100m
        memory: 250Mi
      limits:
        cpu: 500m
        memory: 500Mi
