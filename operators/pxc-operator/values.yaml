images:
  pxcOperator:
    repository: percona/percona-xtradb-cluster-operator
    tag: 1.5.0

config:
  imagePullPolicy: Always
  replicaCount: 1
  pxcOperator:
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi
