{{- if (and .Values.ingress.enabled .Values.config.phpmyadmin) }}
{{- $host := (index .Values.ingress.hosts.phpmyadmin 0) }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "mysql.fullname" . }}-phpmyadmin
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
    {{- if (empty .Values.ingress.certificate) }}
    certmanager.k8s.io/cluster-issuer: {{ .Values.ingress.issuer.name }}
    kubernetes.io/tls-acme: 'true'
    {{- end }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    {{- end }}
  labels:
    app: {{ template "mysql.name" . }}-phpmyadmin
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  rules:
    - host: {{ $host.name }}
      http:
        paths:
          - path: {{ $host.path }}
            backend:
              serviceName: {{ template "mysql.fullname" $ }}-phpmyadmin
              servicePort: http
  {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
  tls:
    - secretName: {{ template "mysql.certificate" . }}
      hosts:
        - {{ $host.name }}
  {{- end }}
{{- end }}
