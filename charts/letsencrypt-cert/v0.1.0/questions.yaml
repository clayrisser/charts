categories:
  - Certificates
questions:

# Settings
  - variable: domain
    default: ''
    description: 'domain name to create wildcard and standard certificate'
    type: string
    required: true
    label: domain
    group: Settings
  - variable: issuer.name
    default: letsencrypt-staging
    description: 'letsencrypt issuer environment'
    type: enum
    options:
      - letsencrypt-staging
      - letsencrypt-prod
    required: true
    label: issuer
