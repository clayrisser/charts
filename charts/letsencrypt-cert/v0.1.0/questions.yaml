categories:
  - Certificates
questions:

# Settings
  - variable: domain
    default: ''
    description: 'domain name to create wildcard and standard certificate'
    type: string
    required: true
    label: domain
    group: Settings
