{{- if (and .Values.ingress.enabled .Values.config.mongo_express.enabled) }}
{{- $host := (index .Values.ingress.hosts.mongo_express 0) }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "mongo.fullname" . }}-mongo-express
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
    {{- if (empty .Values.ingress.certificate) }}
    certmanager.k8s.io/cluster-issuer: {{ .Values.ingress.issuer.name }}
    kubernetes.io/tls-acme: 'true'
    {{- end }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    {{- end }}
  labels:
    app: {{ template "mongo.name" . }}-mongo-express
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  rules:
    - host: {{ $host.name }}
      http:
        paths:
          - path: {{ $host.path }}
            backend:
              serviceName: {{ template "mongo.fullname" $ }}-mongo-express
              servicePort: http
  {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
  tls:
    - secretName: {{ template "mongo.certificate" . }}
      hosts:
        - {{ $host.name }}
  {{- end }}
{{- end }}
