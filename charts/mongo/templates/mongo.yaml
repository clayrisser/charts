apiVersion: kubedb.com/v1alpha1
kind: Mongo
metadata:
  name: {{ template "mongo.fullname" . }}-mongo
  labels:
    app: {{ template "mongo.name" . }}-mongo
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  version: {{ .Values.config.version | quote }}
  configSource:
    configMap:
      name: {{ template "mongo.fullname" . }}-mongo
  {{- if .Values.config.prometheus }}
  monitor:
    agent: prometheus.io/builtin
  {{- end }}
  {{- if .Values.persistence.enabled }}
  storageType: Durable
  storage:
    {{- if .Values.persistence.storageClass }}
    {{- if (eq "-" .Values.persistence.storageClass) }}
    storageClassName: ''
    {{- else }}
    storageClassName: {{ .Values.persistence.storageClass }}
    {{- end }}
    {{- end }}
    accessModes:
      - {{ .Values.persistence.accessMode }}
    resources:
      requests:
        storage: {{ .Values.persistence.size }}
  {{- else }}
  storageType: Ephemeral
  {{- end }}
  {{- if .Values.persistence.backup.enabled }}
  backupSchedule:
    cronExpression: {{ .Values.persistence.backup.schedule | quote }}
    storageSecretName: {{ template "mongo.sharedname" . }}-mongo
    {{ .Values.persistence.backup.type }}:
      {{- if (or (eq "gcs" .Values.persistence.backup.type) (eq "s3" .Values.persistence.backup.type)) }}
      bucket: {{ .Values.persistence.backup.bucket | quote }}
      {{- end }}
      {{- if (eq "s3" .Values.persistence.backup.type) }}
      endpoint: {{ .Values.persistence.backup.endpoint | quote }}
      {{- end }}
      {{- if (eq "azure" .Values.persistence.backup.type) }}
      container: {{ .Values.persistence.backup.container | quote }}
      {{- end }}
      prefix: {{ .Values.persistence.backup.prefix | quote }}
  {{- if (not (empty .Values.persistence.backup.restoreSnapshot)) }}
  databaseSecret:
    {{- if (empty .Values.config.secret) }}
    secretName: {{ template "mongo.sharedname" . }}-mongo-auth
    {{- else }}
    secretName: {{ .Values.config.secret }}
    {{- end }}
  init:
    snapshotSource:
      name: {{ .Values.persistence.backup.restoreSnapshot }}
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- end }}
  serviceTemplate:
    metadata:
      annotations:
        passMe: ToService
    spec:
      type: {{ .Values.service.mongo.type }}
      {{- if (or (eq .Values.service.mongo.type "LoadBalancer") (eq .Values.service.mongo.type "NodePort")) }}
      externalTrafficPolicy: {{ .Values.service.mongo.externalTrafficPolicy }}
      {{- end }}
      ports:
        - name: mongo
          port: 5432
          targetPort: mongo
          {{- if (and (eq .Values.service.mongo.type "NodePort") (not (empty .Values.service.mongo.nodePorts.mongo.port))) }}
          nodePort: {{ .Values.service.mongo.nodePorts.mongo.port }}
          {{- end }}
  replicaServiceTemplate:
    annotations:
      passMe: ToReplicaService
    spec:
      type: {{ .Values.service.mongo.type }}
      {{- if (or (eq .Values.service.mongo.type "LoadBalancer") (eq .Values.service.mongo.type "NodePort")) }}
      externalTrafficPolicy: {{ .Values.service.mongo.externalTrafficPolicy }}
      {{- end }}
      ports:
        - name: mongo
          port: 5432
          targetPort: mongo
          {{- if (and (eq .Values.service.mongo.type "NodePort") (not (empty .Values.service.mongo.nodePorts.mongo.port))) }}
          nodePort: {{ .Values.service.mongo.nodePorts.mongo.port }}
          {{- end }}
  updateStrategy:
    type: {{ .Values.config.updateStrategy }}
  {{- if .Values.persistence.enabled }}
  terminationPolicy: Pause
  {{- else }}
  terminationPolicy: Delete
  {{- end }}
