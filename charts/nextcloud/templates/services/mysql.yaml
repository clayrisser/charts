apiVersion: v1
kind: Service
metadata:
  name: {{ template "nextcloud.fullname" . }}-mysql
  labels:
    app: {{ template "nextcloud.name" . }}-mysql
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: container
  selector:
    app: {{ template "nextcloud.name" . }}-mysql
    release: {{ .Release.Name }}
