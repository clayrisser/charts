{{- if (and .Values.ingress.enabled (and .Values.config.redis.internal .Values.config.phpredisadmin.enabled)) }}
{{- $host := (index .Values.ingress.hosts.phpredisadmin 0) }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "gitlab.fullname" . }}-phpredisadmin
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
    {{- if (empty .Values.ingress.certificate) }}
    certmanager.k8s.io/cluster-issuer: {{ .Values.ingress.issuer.name }}
    kubernetes.io/tls-acme: 'true'
    {{- end }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    {{- end }}
  labels:
    app: {{ template "gitlab.name" . }}-phpredisadmin
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  rules:
    - host: {{ $host.name }}
      http:
        paths:
          - path: {{ $host.path }}
            backend:
              serviceName: {{ template "gitlab.fullname" $ }}-phpredisadmin
              servicePort: http
  {{- if (and .Values.ingress.enabled .Values.ingress.tls) }}
  tls:
    - secretName: {{ template "gitlab.certificate" . }}
      hosts:
        - {{ $host.name }}
  {{- end }}
{{- end }}
