{{- if .Values.ingress.dbtest.enabled }}
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: {{ template "dbtest.fullname" . }}-dbtest
  annotations:
    kubernetes.io/ingress.class: nginx
    {{- if .Values.ingress.dbtest.tls }}
    {{- if (empty .Values.ingress.dbtest.certificate) }}
    certmanager.k8s.io/cluster-issuer: {{ .Values.ingress.dbtest.issuer.name }}
    kubernetes.io/tls-acme: 'true'
    {{- end }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    {{- end }}
  labels:
    app: {{ template "dbtest.name" . }}-dbtest
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  rules:
    - host: {{ .Values.ingress.dbtest.hostname }}
      http:
        paths:
          - path: {{ .Values.ingress.dbtest.path }}
            backend:
              serviceName: {{ template "dbtest.fullname" . }}-dbtest
              servicePort: http
  {{- if .Values.ingress.dbtest.tls }}
  tls:
    - secretName: {{ template "dbtest.dbtest-certificate" . }}
      hosts:
        - {{ .Values.ingress.dbtest.hostname }}
  {{- end }}
{{- end }}
