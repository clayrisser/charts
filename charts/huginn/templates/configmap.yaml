apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "huginn.fullname" . }}
  labels:
    app: {{ template "huginn.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  {{- $postgres := .Values.config.postgres }}
  {{- if (empty $postgres.host) }}
  postgres_host: {{ template "huginn.fullname" . }}-postgres
  postgres_port: 5432
  {{- else }}
  postgres_host: {{ $postgres.host | quote }}
  postgres_port: {{ $postgres.port | quote }}
  {{- end }}
  postgres_database: {{ $postgres.database | quote }}
  postgres_username: {{ $postgres.username | quote }}
