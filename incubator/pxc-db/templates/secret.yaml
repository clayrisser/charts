apiVersion: v1
kind: Secret
metadata:
  name: {{ template "pxc-db.fullname" . }}
  labels:
    app: {{ template "pxc-db.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
stringData:
  {{- $mysql := .Values.config.mysql }}
  mysql_password: {{ $mysql.password | quote }}
  mysql_url: {{ (include "pxc-db.mysql-url" .) | quote }}
