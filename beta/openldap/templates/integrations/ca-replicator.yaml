{{- if (and .Values.service.openldap.tls.enabled (not .Values.service.openldap.tls.certificate)) }}
{{- $name := (printf "%s-replicator" .Values.service.openldap.tls.issuer.name) }}
{{- $namespace := "kube-system" }}
apiVersion: integration.siliconhills.dev/v1alpha2
kind: Plug
metadata:
  name: ca-replicator
  labels:
    app: {{ template "openldap.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  interfaceVersions: '*'
  interface:
    name: replicator
    namespace: {{ $namespace }}
  socket:
    name: {{ $name }}
    namespace: {{ $namespace }}
  config:
    caName: ca
{{- end }}
