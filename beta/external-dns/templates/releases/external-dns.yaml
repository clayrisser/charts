apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: {{ template "external-dns.name" . }}
  labels:
    app: {{ template "external-dns.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  forceUpgrade: false
  helmVersion: v3
  chart:
    repository: https://charts.bitnami.com/bitnami
    name: bitnami
    version: 5.4.0
  values:
    image:
      repository: {{ .Values.images.externalDns.repository | quote }}
      tag: {{ .Values.images.externalDns.tag | quote }}
    provider: cloudflare
    cloudflare:
      apiKey: {{ .Values.cloudflare.apiKey | quote }}
      email: {{ .Values.cloudflare.email | quote }}
      proxied: false
      sources:
        - ingress
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: kubernetes.io/arch
                  operator: In
                  values:
                    - amd64
