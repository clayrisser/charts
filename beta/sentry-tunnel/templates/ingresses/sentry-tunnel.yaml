{{- if .Values.ingress.sentryTunnel.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ template "sentry-tunnel.name" . }}
  labels:
    app.kubernetes.io/name: {{ template "sentry-tunnel.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    {{- if .Values.ingress.sentryTunnel.tls }}
    {{- if (not .Values.ingress.sentryTunnel.certificate) }}
    kubernetes.io/tls-acme: 'true'
    {{- if .Values.ingress.sentryTunnel.issuer.name }}
    cert-manager.io/cluster-issuer: {{ .Values.ingress.sentryTunnel.issuer.name }}
    {{- end }}
    {{- end }}
    nginx.ingress.kubernetes.io/ssl-redirect: 'true'
    {{- end }}
spec:
  rules:
    - host: {{ .Values.ingress.sentryTunnel.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sentry-tunnel
                port:
                  name: http
  {{- if .Values.ingress.sentryTunnel.tls }}
  tls:
    - secretName: {{ template "sentry-tunnel.sentry-tunnel-certificate" . }}
      hosts:
        - {{ .Values.ingress.sentryTunnel.hostname }}
  {{- end }}
{{- end }}
