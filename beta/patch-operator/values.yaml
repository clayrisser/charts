config:
  imagePullPolicy: IfNotPresent
  updateStrategy: RollingUpdate
  debug: false
  patchOperator:
    baseUrl: ""
    hostname: ""
    resources:
      enabled: defaults
      requests:
        cpu: 1m
        memory: 16Mi
      limits:
        cpu: 50m
        memory: 128Mi

images:
  patchOperator:
    repository: registry.gitlab.com/risserlabs/community/patch-operator
    tag: 0.1.0
