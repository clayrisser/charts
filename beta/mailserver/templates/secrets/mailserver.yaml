apiVersion: v1
kind: Secret
metadata:
  name: {{ template "mailserver.fullname" . }}-secrets
  labels:
    app: {{ template "mailserver.name" . }}-secrets
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
stringData:
