apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Release.Name }}-docker-mailserver-secrets
  labels:
    app: {{ .Release.Name }}-{{ .Release.Name }}-docker-mailserver-secrets
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
stringData:
  {{ template "mailserver.mailserver-domain" . }}-mail.private: {{ .Values.config.privateKey | default (genPrivateKey) }}
