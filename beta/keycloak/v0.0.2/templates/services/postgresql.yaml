apiVersion: v1
kind: Service
metadata:
  name: keycloak-db-secret
  labels:
    app: keycloak-db-secret
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ExternalName
  externalName: {{ .Values.config.postgres.host | quote }}
