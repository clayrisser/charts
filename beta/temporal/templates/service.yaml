apiVersion: v1
kind: Service
metadata:
  name: temporal-server
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 7233
  selector:
    app: temporal-server
