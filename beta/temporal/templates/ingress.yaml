apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: temporal-ingress
spec:
  rules:
  - host: temporal.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: temporal-server
            port:
              number: 80
