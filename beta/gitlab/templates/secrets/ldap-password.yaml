apiVersion: v1
kind: Secret
metadata:
  name: openldap-secret
  labels:
    app: {{ template "gitlab.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
type: Opaque
data:
  admin: \{\{ .Values.openldapPassword | b64enc }}
