{{- if .Values.persistence.velero.enabled }}
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: {{ template "gitlab.name" . }}
  labels:
    app: {{ template "gitlab.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  schedule: {{ .Values.persistence.velero.schedule | quote }}
  template:
    includedNamespaces:
      - {{ .Release.Namespace }}
    ttl: {{ .Values.persistence.velero.ttl | quote }}
---
apiVersion: replicator.siliconhills.dev/v1alpha1
kind: Replicator
metadata:
  name: backup
  labels:
    app: {{ template "gitlab.name" . }}-backup
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
 from:
   apiVersion: velero.io/v1
   kind: Schedule
   name: {{ template "gitlab.name" . }}
   namespace: {{ .Release.Namespace }}
 to:
   name: {{ template "gitlab.name" . }}.{{ .Release.Namespace }}
   namespace: velero
{{- end }}
