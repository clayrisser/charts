image:
  name: registry.gitlab.com/bitspur/rock8s/images/kube-commands:3.18
  entrypoint: ['']

variables:
  GITLAB_TOKEN: ''

stages:
  - publish

pages:
  stage: publish
  script:
    # - make -s prev-artifacts
    - make -s package
  artifacts:
    paths:
      - public
  only:
    - main
